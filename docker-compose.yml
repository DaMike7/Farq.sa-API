version: "3.9"

services:
  db:
    image: postgis/postgis:16-3.4
    container_name: farq_postgis
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-farq}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-farqpass}
      POSTGRES_DB: ${POSTGRES_DB:-farq_db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-farq} -d ${POSTGRES_DB:-farq_db}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - farq_net

  redis:
    image: redis:7-alpine
    container_name: farq_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - farq_net

  api:
    build: .
    container_name: farq_api
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_DB=0
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
    networks:
      - farq_net

  worker:
    build: .
    container_name: farq_worker
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_DB=0
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./uploads:/app/uploads
    command: celery -A celery_worker.worker.celery_app worker --loglevel=info --pool=solo
    networks:
      - farq_net

volumes:
  postgres_data:
  redis_data:

networks:
  farq_net:
    driver: bridge